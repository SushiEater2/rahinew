// Firebase has been disabled
// All authentication and data storage is now handled by the backend API

// Mock exports to prevent import errors
export const auth = null;
export const db = null;
export const storage = null;

const app = null;
export default app;
